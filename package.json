{
  "name": "codecave-website",
  "version": "1.0.0",
  "private": true,
  "description": "CodeCave Inc. website — static frontend + Convex backend",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "dev": "npx serve . -l 3000"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.80",
    "convex": "^1.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
